* Understand why there're loops when using *simple_switch* with the *dc_ha_root* topology

* Learn the code for *simple_switch_stp* controller application

* Provide automatic controller startup from MN script
